[project]
name = "get-key"
version = "0.1.0"
description = "Vertex AI authentication and API key generator"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.31.0",
    "cryptography>=41.0.0",
]

[tool.uv]
package = false